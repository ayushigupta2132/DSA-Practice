//int arr[n]
//If you declare an array inside int main..then it is assigned by garbage values
//If you declare an array above int main i.e globally...all values will be assigned to 0
//Max size is int arr[10^6]....inside int main
//Max size is int arr[10^7]....globally
//array indexing is from 0 to n-1
//we don't know the address of the 0th index of array
//but we're sure that the after elements will be in contiguous addresses
