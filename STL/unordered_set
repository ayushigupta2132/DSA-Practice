#include<iostream>
#include<unordered_set>
using namespace std;
int main(){
    //UNIQUE and RANDOM
    //lower bound and upper bound does not work
    //rest all unctions are same as set
    //better complexity....0(1)
    //in the worst case it is 0(n)
}